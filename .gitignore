# ── Binary / Build ──
/bin/
/uwas
/uwas.exe
*.test.exe

# ── Go ──
*.test
vendor/

# ── Coverage ──
*.out
*.cov
cov_*
cover_*
*_coverage.html
coverage.html
coverage.txt

# ── IDE ──
.idea/
.vscode/
*.swp
*.swo
*~

# ── OS ──
.DS_Store
Thumbs.db

# ── Config with secrets (not the example) ──
/uwas.yaml
!uwas.example.yaml

# ── Runtime data ──
*.pid
*.log
/cache/
/certs/

# ── Node modules ──
web/dashboard/node_modules/
docs/site/node_modules/

# ── Build output (site) ──
docs/site/dist/

# ── Dashboard build output ──
# NOTE: dist/ is now committed to allow release builds to include the dashboard

# ── Demo / temp ──
demo/

# ── Playwright screenshots ──
.playwright-mcp/

# ── Profiling ──
*.prof

# ── Commit message temp ──
.commitmsg.tmp
.git_msg.tmp
.git_commit_msg.tmp

# ── Dev / scratch (DFMT workarounds, ad-hoc scripts) ──
.tmp_*.js
.tmp_*.json
run-tests.js
commit.bat
.npm/

.dfmt/